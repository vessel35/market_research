# Phase 4 reproduction environment — run 20260613_0558.
# Python 3.12 (tested 3.12.13). Recreate:
#   python3.12 -m venv .venv && . .venv/bin/activate
#   pip install -r reproducibility/requirements.txt
# Versions are pinned to the executed run (also recorded in stage_a_current_regime/outputs/provenance.json
# and stage_b_prediction/outputs/prediction_provenance.json).
pandas==2.1.4
numpy==1.26.3
scipy==1.16.3
scikit-learn==1.9.0
lightgbm==4.6.0
xgboost==2.1.4
