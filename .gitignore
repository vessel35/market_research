# Local Claude Code overrides / secrets (never commit)
.claude/settings.local.json
*.local.json
.env
.env.*
!.env.example
*.key
*.pem
*.p12
*.keystore
secrets/
credentials/

# OS / editor
.DS_Store
*.swp
*~

# Python
__pycache__/
*.pyc
.ipynb_checkpoints/

# Large regenerable Stage B feature/label intermediates (~72MB) — deterministic via
# build_prediction_labels.py; full per-class metrics are kept in prediction_metrics.json.
reports/phase4_market_regime/*/prediction_matrix_*.csv.gz
